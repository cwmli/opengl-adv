#ifndef TEXTURE_H
#define TEXTURE_H

#include <glew.h>
#include <string>
#include <fstream>
#include <sstream>
#include <iostream>
#include <vector>
#include <algorithm>

class Texture{
public:
	GLuint textureSource;
	Texture(const GLchar*);
private:
	GetTextureSource
};

#endif